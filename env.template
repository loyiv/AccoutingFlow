# 说明：由于某些环境会忽略/屏蔽 `.env*` 文件，本项目用 `env.template` 作为示例。
# 使用时你可以复制为 `env.local`，再在启动命令里显式加载，或直接在 docker-compose.yml 中改环境变量。

# ---- PostgreSQL (docker-compose) ----
POSTGRES_DB=accountingflow
POSTGRES_USER=accountingflow
POSTGRES_PASSWORD=accountingflow

# ---- Backend ----
DATABASE_URL=postgresql+psycopg://accountingflow:accountingflow@db:5432/accountingflow
JWT_SECRET=dev-secret-change-me
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=720
CORS_ORIGINS=http://localhost:5173
STORAGE_DIR=./storage

# ---- Seed Users ----
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=admin123
SEED_ACCOUNTANT_USERNAME=accountant
SEED_ACCOUNTANT_PASSWORD=accountant123
SEED_MANAGER_USERNAME=manager
SEED_MANAGER_PASSWORD=manager123


